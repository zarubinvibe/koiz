# 安全与隐私

<p align="center"><img src="docs/assets/pantheon/doc-security.png" alt="一块开着长方形通孔的大理石板，立在进来的蓝色路径和桌上那摞大理石片之间" width="100%"></p>

## Koiz 保存什么

Koiz 只保存一个文件：教训日志，路径是 `~/.claude/koiz/lessons.jsonl`。每一行是一条记录：失败的文字、原因、固定它的机制、项目名和日期。除此之外，任何地方都不留东西。

日志故意放在项目仓库之外。教训里带着文件路径、命令行和别人项目的名字，公开的代码树不是它该待的地方。你克隆这个仓库之后，机器上出现的那份日志只属于你，也不会被提交。

## 什么会离开你的机器

什么都不会。这个仪器没有网络调用，也没有依赖。它读文件，写一个文件，然后退出。采集读的是本来就在你磁盘上的会话记录，不会把它复制到任何地方。

## 密钥

密钥过滤放在写入这一侧，不是读取这一侧。令牌、API 密钥、AWS 访问密钥或 PEM 私钥，在进入日志之前就被替换成标记。没有被写下来的东西，之后也不可能被不小心展示出来。

这个过滤器有自检和变异测试兜着：把过滤关掉，自检必须失败。如果哪天这个变异活了下来，说明检查已经不再检查，这被当作缺陷处理。

## 仪器会对你的系统做什么

`install.sh` 写两样东西：目录 `~/.claude/koiz`，以及 `~/.claude/settings.json` 里的一条 `SessionEnd` 记录。改动之前它会先备份设置文件，文件解析不了就干脆不写。除非你加了 `--yes`，否则每一步之前它都会先问你。

系统里别的东西一概不动。没有守护进程，没有启动项，没有后台任务。

## 发现漏洞往哪里写

到 https://github.com/zarubinvibe/koiz/issues 开一个 issue，写清楚你运行了什么、看到了什么。如果这个发现会暴露别人的数据，就把敏感部分省掉再写，并说明你手里有；维护者会来找你要一个私下的渠道。
